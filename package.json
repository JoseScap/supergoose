{
  "name": "supergoose",
  "private": true,
  "version": "0.0.1",
  "license": "SEE LICENSE IN LICENSE",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test --import tsx packages/core/test/*.test.ts packages/infra/test/*.test.ts packages/api/test/*.test.ts",
    "test:coverage": "c8 --check-coverage --lines 90 --statements 90 --branches 75 --functions 80 --reporter=text --reporter=lcov --reporter=html node --test --import tsx packages/core/test/*.test.ts packages/infra/test/*.test.ts packages/api/test/*.test.ts",
    "coverage": "npm run test:coverage",
    "dev:api": "npm run dev --workspace supergoose-api",
    "dev:dashboard": "npm run dev --workspace supergoose-dashboard",
    "dev": "concurrently -k -n api,dashboard -c green,cyan \"npm run dev:api\" \"npm run dev:dashboard\"",
    "build": "npm run build --workspace supergoose-core && npm run build --workspace supergoose-infra && npm run build --workspace supergoose-dashboard && npm run build --workspace supergoose-api",
    "start": "npm run start --workspace supergoose-api",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@types/node": "^26.2.0",
    "@typescript-eslint/eslint-plugin": "^8.40.0",
    "@typescript-eslint/parser": "^8.40.0",
    "c8": "^12.0.0",
    "concurrently": "^9.2.4",
    "eslint": "^9.34.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
